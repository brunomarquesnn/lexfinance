{
  "name": "lexfinance",
  "version": "1.0.0",
  "description": "LexFinance - Sistema de Gestão Financeira Jurídica",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx prisma generate && npx prisma db push",
    "dev": "node server.js",
    "seed": "node prisma/seed.js",
    "db:push": "npx prisma db push",
    "db:generate": "npx prisma generate",
    "setup": "npx prisma generate && npx prisma db push && node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "date-fns": "^3.6.0",
    "exceljs": "^4.4.0",
    "express": "^4.21.0",
    "express-session": "^1.19.0",
    "node-forge": "^1.3.3",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
